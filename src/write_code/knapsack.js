/**
 * A thief robbing a store finds n items. 
 * The i'th item is worth v(i) dollars and weights w(i) pounds, 
 * where v(i) and w(i) are integers. 
 * The thief wants to take as valuable a load as possible,
 * but he can carry at most W pounds in his knapsack, integer W.
 */
